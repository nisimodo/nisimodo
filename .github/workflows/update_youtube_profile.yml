name: Update YouTube Profile Image

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.REPO_PUSH_TOKEN }}
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate profile image
        env:
          YOUTUBE_API_KEY: ${{ "AIzaSyCX3JaQV5iy2IrG3t15FOGuul3xIxKK6oE" }}
        run: |
          python scripts/generate_youtube_profile.py --query nisimodo --output assets/profile.png --readme README.md

      - name: Commit and push
        env:
          PUSH_TOKEN: ${{ secrets.REPO_PUSH_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${PUSH_TOKEN}@github.com/nisimodo/nisimodo.git
          git add assets/profile.png README.md || true
          if ! git diff --cached --quiet; then
            git commit -m "Update YouTube profile image"
            git push
          else
            echo "No changes to commit"
          fi
